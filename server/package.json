{
  "name": "dungeon-boss-matchmaker",
  "version": "0.1.0",
  "description": "Matchmaking + lockstep relay server for Dungeon Boss online play. Pairs waiting players into tables automatically and sequences their move messages. Runs no game rules.",
  "type": "module",
  "main": "src/index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
